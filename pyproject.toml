[project]
name = "tobox-service"
version = "0.1.0"
description = "A simple FastAPI service with hello endpoint"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
]

[dependency-groups]
dev = []
